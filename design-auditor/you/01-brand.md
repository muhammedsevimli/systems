# You · brand and boundaries

> Fill this in once, then forget it. The system reads it before every audit.
> You can leave it empty. If it is empty the system runs a brandless audit and treats changing colors and typefaces as fair game.

## Page address (optional)
If the page is live, write its address here. With an address the system opens it in a browser and verifies the measurements. Without one it works from the screenshot alone, which is enough.

```
address:
```

## Brand colors (do not touch)
If your brand has colors, write them here. The system will not suggest changing them; if one fails the contrast test it suggests changing the color of the text on it, or where that color is used, instead.

```
primary:
secondary:
text:
background:
```

## Typefaces (do not touch)
```
heading typeface:
body typeface:
```

## What this page is for
One sentence. The system uses it to answer "which button should be the primary one".

```

```

## Who is looking at it
One sentence. For example "a small business owner on their phone, in a hurry". The system reads mobile priority and tap target strictness from this.

```

```

## Anything else that must not change
Logo placement, the layout of a particular section, the formatting of a legal notice.

```
-
```

---

## My repeating mistakes
> You do not fill this in. When the system sees the same mistake a second time it writes it here with a date. Over time it becomes the list of your own blind spots.

(empty)
